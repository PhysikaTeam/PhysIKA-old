# file(GLOB header *.h)
# file(GLOB source *.cc *.c *.cpp *.cu)

# if (${APPLE})
#     add_library(ZJU_GEOMETRY SHARED ${source})
# else ()
#     cuda_add_library(ZJU_GEOMETRY SHARED ${source})
# endif(${APPLE})
