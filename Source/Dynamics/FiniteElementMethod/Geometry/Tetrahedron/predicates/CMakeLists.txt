add_library(pred STATIC predicates.cpp)
#TARGET_INCLUDE_DIRECTORIES(pred PUBLIC
#$<BUILD_INTERFACE:.>)
SET_TARGET_PROPERTIES(pred
  PROPERTIES
  ARCHIVE_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/lib"
  LIBRARY_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/lib"
  RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/bin")


install_as_LIB(pred)