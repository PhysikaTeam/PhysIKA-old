#include <fstream>
#include <cassert>
#include <vector>
#include <string>
void load_field_data(std::string const file_name, std::vector<double> &field, size_t &x_cells, size_t &y_cells);
void run(string *height,string *surface,string *vx.string *vy);
